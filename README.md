# One Piece Bot (MVP)
Step-by-step build of a tiny retrieval chatbot.
Run:
1) python -m venv .venv
2) .\.venv\Scripts\Activate.ps1
3) pip install fastapi uvicorn[standard] scikit-learn
4) uvicorn app:app --reload
